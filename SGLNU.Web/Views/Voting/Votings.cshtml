@using Microsoft.AspNetCore.Identity.UI.V4.Pages.Account.Manage.Internal
@using System.Diagnostics.Eventing.Reader
@using SGLNU.BLL.DTO
@using SGLNU.DAL.Entities
@{
    Layout = "_Layout";
}
<section class="services section container" id="services">
    <h2 class="section__title">Голосування</h2>
    <ul class="list-group-horizontal">

        <li class="list-group-item list-group-item-action">
            <a asp-action="Edit" asp-route-id=0 asp-controller="Voting" class="nav__link" style="color:blue">
                new ->
            </a>
        </li>
        @foreach (VotingDTO item in Model.Votings)
        {
            <li class="list-group-item list-group-item-action">
                <a asp-action="View" asp-route-id=@item.Id asp-controller="Voting" class="nav__link">
                    <b>@item.Faculty.Name</b> - "@item.Title" starts in @item.StartDate.ToShortDateString()
                </a>
                @if (item.IsActive)
                {
                    <a asp-action="DeActivate" asp-route-id=@item.Id asp-controller="Voting" class="nav__link" style="color:blue">
                        <b>Deactivate</b>
                    </a>
                }
                else
                {
                    <a asp-action="Activate" asp-route-id=@item.Id asp-controller="Voting" class="nav__link" style="color:blue">
                        <b>Activate</b>
                    </a>
                    <a asp-action="Edit" asp-route-id=@item.Id asp-controller="Voting" class="nav__link" style="color:blue">
                        <b>EDIT</b>
                    </a>
                    <a asp-action="Delete" asp-route-id=@item.Id asp-controller="Voting" class="nav__link" style="color:blue">
                        <b>DELETE</b>
                    </a>
                }
            </li>
        }

    </ul>
</section>